import React from 'react';
import {
  TouchableOpacity,
  Button,
  Text,
  SafeAreaView,
  StyleSheet,
  TextInput,
  View,
} from 'react-native';
const Main = ({navigation}) => {
  return (
    <SafeAreaView>
      <Text>Main</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  signup: {
    marginTop: 10,
  },
});

export default Main;
